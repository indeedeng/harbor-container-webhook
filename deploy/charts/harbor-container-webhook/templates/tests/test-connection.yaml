apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harbor-container-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "harbor-container-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harbor-container-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
